export enum ButtonType {
  Button = "button",
  Reset = "reset",
  Submit = "submit",
}

export enum ButtonVariant {
  Default = "default",
  Outlined = "outlined",
  Text = "text",
  Elevated = "elevated",
}
